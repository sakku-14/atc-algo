#include <stdio.h>

int main(void){
    int i=0, counter = 0;
    char line[100000];
    scanf("%s", line);

    while(line[i] != '\0'){
        if(line[i] >= 'A' && line[i] <= 'Z'){

        }
        i++;
    }
    i=0;

    return 0;
}
